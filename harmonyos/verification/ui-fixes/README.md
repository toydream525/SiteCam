# 界面问题修复和验证 — 2026-09-10

## 已修复

1. 相册闪退：设备崩溃日志指向 Index.galleryDetailContent，错误为 Cannot read property bind of undefined。用箭头函数传递 BuilderParam，保持父页面上下文。参考[华为 BuilderParam 文档](https://developer.huawei.com/consumer/cn/doc/doccenter-capabilities/arkts-builderparam)。
2. 图标：重画越界的齿轮路径；补齐 8 个标注工具及撤销/重做 SVG 的 viewBox。
3. 输入框：检查并设置全部 15 个 TextInput、1 个 TextArea 的正文、占位符、背景和光标颜色。水印快速输入使用 13 fp，选中开关与滑块统一黄色。
4. 弹窗：9 个自定义弹窗入口全部启用 customStyle，消除默认浅色底板；未包裹滚动容器的表单已补齐最大高度和滚动。应用统一深色模式，系统菜单和确认框使用一致的深色底色。

## 实测通过

- SiteCamPhoneQA 普通手机 360×800 vp / API 26：安装、启动、相册列表 → 照片详情 → 返回列表、方向弹窗、离线教程、水印样式取消、当前工程保留（phone-smoke.json）。
- SiteCamFoldQA 外屏约 345×801 vp / API 26：同样的界面回归通过（fold-smoke.json）。
- 独立测试模拟器：新建、编辑工程、导出选择、批量分类、日期筛选、问题记录弹窗实际打开和取消通过；输入框及齿轮截图已人工检查。
- 展开至约 711 vp：相册状态保留，照片详情打开，编辑页裁剪打开/取消、水平翻转、旋转和放弃修改通过。
- 手机修复后的崩溃目录仍只有修复前 18:58—18:59 的 3 条日志；独立测试模拟器无 SiteCam 崩溃日志。
- 同一应用源码 Debug、Release 构建通过；普通手机模拟器已安装修复后的 Debug 包。
- 核心运行验证 36 项，35 项通过，覆盖八款水印、图片合成、编辑、数据、导出及方向/布局规则（runtime.json）。

## 未计为通过

- 原生视频水印与音轨：模拟器没有可用 H.264 编解码器，环境阻塞。
- HarmonyOS 5/6 真机、硬件拍摄质量、全部平板/分屏/大字号组合仍需对应环境实测。
- 本轮导出弹窗检查是打开/取消；不能等同于每一种系统文件选择器、分享接收应用及相册授权组合均已验收。

截图仅保留设置、输入面板和工程表单；摄像头画面不作为交付截图。

补充：六种标注工具逐项切换及退出通过；展开屏编辑预览曾靠左，已修复容器宽度。展开与折叠后分别检查图片中心位于窗口中心，编辑页面保留（editor-layout.json 记录展开边界）。最终包已再次覆盖安装并启动普通手机模拟器。
